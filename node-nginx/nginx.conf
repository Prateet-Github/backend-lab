events {}

http {
    upstream backend {
        least_conn;
        server app-1:5001;
        server app-2:5001;
        server app-3:5001;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}